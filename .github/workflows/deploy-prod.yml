# 63번 prod 자동 배포.
#
# main 이 움직이면 (또는 수동 trigger 시) 63번 self-hosted runner
# (label: hc63-prod) 가 PROD_DIR 에서 IMAGE_TAG 갱신 + docker compose
# pull && up -d 실행.
#
# 63번에는 dev (hypercube project, hc-* 컨테이너) 와 prod (hypercube-prod
# project, hcprod-* 컨테이너) 가 한 머신에서 공존. 같은 GPU·네트워크 인프라
# 공유, 다른 docker compose project 로 격리.
#
# Migration 은 backend container entrypoint 가 자동 처리 (manage.py migrate).
#
# 수동 실행: GitHub Actions 탭 → "Deploy to 63 prod" → Run workflow.
#  - sha 입력란에 특정 commit 으로 pin 가능. 비우면 main HEAD.

name: Deploy to 63 prod

on:
  workflow_run:
    workflows: ["Build and push images"]
    types: [completed]
    branches: [main]
  workflow_dispatch:
    inputs:
      sha:
        description: "Image short SHA to deploy (e.g. 14e92fa). Empty = main HEAD."
        required: false
        default: ""

jobs:
  deploy:
    # 자동 trigger 면 build 가 success 일 때만. 수동 trigger 는 무조건.
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: [self-hosted, hc63-prod]
    timeout-minutes: 15

    env:
      PROD_DIR: /docker/hypercube-prod
      HEALTHCHECK_URL: http://localhost:37003/api/health/

    steps:
      - name: Determine target SHA
        id: target
        run: |
          set -euo pipefail
          INPUT_SHA="${{ github.event.inputs.sha || '' }}"
          if [ -n "$INPUT_SHA" ]; then
            SHORT="${INPUT_SHA:0:7}"
          elif [ "${{ github.event_name }}" = "workflow_run" ]; then
            FULL="${{ github.event.workflow_run.head_sha }}"
            SHORT="${FULL:0:7}"
          else
            FULL="${GITHUB_SHA}"
            SHORT="${FULL:0:7}"
          fi
          echo "short=$SHORT" >> "$GITHUB_OUTPUT"
          echo "Target image tag: sha-$SHORT"

      - name: Update IMAGE_TAG and pull
        working-directory: ${{ env.PROD_DIR }}
        run: |
          set -euo pipefail
          BACKUP=".env.bak.$(date +%s)"
          cp .env "$BACKUP"
          sed -i "s|^IMAGE_TAG=.*|IMAGE_TAG=sha-${{ steps.target.outputs.short }}|" .env
          echo "Updated .env (backup: $BACKUP):"
          grep IMAGE_TAG .env
          docker compose pull

      - name: Apply
        working-directory: ${{ env.PROD_DIR }}
        run: docker compose up -d

      - name: Wait for backend healthy
        run: |
          set -euo pipefail
          for i in $(seq 1 24); do
            if curl -sf "$HEALTHCHECK_URL" > /dev/null; then
              echo "Healthy after ${i} attempts ($(($i * 5))s)."
              exit 0
            fi
            sleep 5
          done
          echo "Health check failed. Backend logs:"
          docker logs hcprod-backend --tail 80 || true
          exit 1

      - name: Migration log tail
        if: always()
        run: docker logs hcprod-backend --tail 30 2>&1 | grep -E "Applying|migrat|ERROR" | tail -20 || true

      - name: Summary
        if: always()
        run: |
          echo "### Deploy to 63 prod" >> "$GITHUB_STEP_SUMMARY"
          echo "- Image tag: \`sha-${{ steps.target.outputs.short }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- URL: $HEALTHCHECK_URL" >> "$GITHUB_STEP_SUMMARY"
          echo "- Status: ${{ job.status }}" >> "$GITHUB_STEP_SUMMARY"
