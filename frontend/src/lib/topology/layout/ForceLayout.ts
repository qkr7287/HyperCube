/**
 * Wraps d3-force-3d simulation.
 * Owns forces (center, charge, link, collide) and runs the layout
 * in a worker-free, per-tick fashion so we can throttle when idle.
 * Phase 1.
 */

export class ForceLayout {
	// Phase 1 implementation
}
