/**
 * Abstract hub base — knows its members, its center-of-mass, and
 * how to render its own mesh. Subclasses specialize the mesh + color.
 *
 * Phase 1 (base + StackHub), Phase 3 (Network / Volume).
 */

export abstract class Hub {
	// Phase 1 implementation
}
