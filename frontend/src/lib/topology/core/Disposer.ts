/**
 * three.js resource cleanup helper.
 * Every mesh / geometry / material / texture passes through here
 * so the scene never leaks GPU memory on server switch or unmount.
 *
 * Phase 1.
 */

export class Disposer {
	// Phase 1 implementation
}
