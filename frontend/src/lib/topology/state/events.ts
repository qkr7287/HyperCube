/**
 * External-to-scene event bus. Sidebar emits click events here and
 * Topology reacts (focusContainer / focusHub / resetFocus).
 * Phase 4.
 */

export const topologyEvents = null; // placeholder, implemented in Phase 4
