/**
 * Svelte store — single source of truth for:
 *   - hub visibility toggles (stack / network / volume)
 *   - selected entity id
 *   - pinned node set
 * UI bindings (toggle switches, sidebar sync) subscribe here.
 * Phase 3 (toggles), Phase 2 (selection).
 */

export const topologyStore = null; // placeholder, implemented in later phases
