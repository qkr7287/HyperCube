/**
 * Base class for hub↔container connections. Subclasses pick
 * line material (solid/dotted/dashed) and color.
 * Phase 1 (base + stack), Phase 3 (network/volume).
 */

export abstract class Connection {
	// Phase 1 implementation
}
